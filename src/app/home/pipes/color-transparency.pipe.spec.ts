import { ColorTransparencyPipe } from './color-transparency.pipe';

describe('ColorTransparencyPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorTransparencyPipe();
    expect(pipe).toBeTruthy();
  });
});
